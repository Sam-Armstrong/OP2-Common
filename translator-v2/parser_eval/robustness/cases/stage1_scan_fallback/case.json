{
  "name": "stage1_scan_fallback",
  "category": "pipeline",
  "description": "Stage-1 Flang fallback when op2-flang-scan fails, then fparser2 succeeds",
  "notes": "Uses a local fail_scan.sh stub as --flang-scan; source is otherwise ordinary OP2 Fortran",
  "sources": ["stage1_scan_fallback.F90"],
  "targets": ["seq"],
  "translator_flags": ["--flang-scan", "./fail_scan.sh"],
  "expect_fparser2": "pass",
  "expect_flang": "fallback"
}
